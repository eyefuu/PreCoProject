<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Booking Rooms</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Thai:wght@100..900&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Noto Sans Thai', sans-serif;
            /* Change font-family to Noto Sans Thai */
        }

        .navbar {
            padding-top: 0px;
        }

        .card {
            width: 80%;
            margin: 1em 10% 1em 10%;
        }
    </style>
</head>

<body>


    <nav class="navbar navbar-expand-lg bg-body-tertiary ">
        <div class="container-fluid text-bg-dark p-3 ">
            <!-- <a class="navbar-brand " href="#">ระบบจองห้องประชุม
          </a> -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active text-white" aria-current="page" href="../user-home">หน้าหลัก</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active text-white" aria-disabled="true" href="./bookings">ประวัติการจอง</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link disabled text-white-50" aria-current="page">ห้องประชุม</a>
                    </li>
                </ul>
                <form class="d-flex" role="search">
                    <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-light me-2" type="submit">ค้นหา</button>
                </form>
                <button type="button" class="btn btn-danger" onclick="logout()">logout</button>

                <script>
                    function logout() {
                        window.location.href = "/logout";
                    }
                </script>

            </div>
        </div>
    </nav>
    <h2>ห้องประชุม</h2>

    <!-- <div class="card text-center">
        <div class="card-header">
            Featured
        </div>
        <div class="card-body">
            <h5 class="card-title">Special title treatment</h5>
            <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
            <a href="#" class="btn btn-primary">Go somewhere</a>
        </div>
    </div> -->

    <% let currentRoomID = ''; %>
<% availableTimeslots.forEach((timeslot, index) => { %>
    <% if (timeslot.roomID !== currentRoomID) { %>
        <% if (index !== 0) { %>
            </ul>
        </div>
    </div>
        <% } %>
    <div class="card text-center">
        <div class="card-header">
            ห้องประชุม :<%= timeslot.roomname %>
        </div>
        <div class="card-body">
            <ul>
        <% currentRoomID = timeslot.roomID; %>
    <% } %>

    <li>
        <% if (timeslot.statustime === '2') { %>
            <p>วันที่: <%= timeslot.formattedDate %> เวลา: <%= timeslot.startTime %> - <%= timeslot.endTime %></p>
            <p>สถานะ: ไม่ว่าง </p>
        <% } else { %>
            <p>วันที่: <%= timeslot.formattedDate %> เวลา: <%= timeslot.startTime %> - <%= timeslot.endTime %></p>
            <p>สถานะ: ว่าง </p>
        <% } %>
    </li>

    <% if (index === availableTimeslots.length - 1 || timeslot.roomID !== availableTimeslots[index + 1].roomID) { %>
        </ul>
    </div>
    </div>
    <% } %>
<% }); %>




</body>

</html>

